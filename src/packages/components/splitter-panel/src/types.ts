
export interface SplitterPanelProps {
    // 尺寸
    size?: number | string
    // 最大尺寸
    max?: number | string
    // 最小尺寸
    min?: number | string
    // 是否可拖动
    // resizable?: boolean
    // 是否可折叠
    collapsible?: boolean
}
